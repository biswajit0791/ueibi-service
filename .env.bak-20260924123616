NODE_ENV=development
PORT=4000

# postgresql://USER:PASSWORD@HOST:

DATABASE_URL="postgresql://ueibi-dbuser:10de844ed92eedb6187284a3b511795ba7ea138bfc546a15d7e0d137f0b8e025@136.85.25.34:5432/ueibi-defigo-db"


FRONTEND_ORIGIN=http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# Email (SMTP / Zoho / SendGrid / Console)
MAIL_TRANSPORT=smtp
MAIL_HOST=mail.ueibi.com
MAIL_ENCRYPTION=ssl
MAIL_PORT=465
MAIL_USERNAME=administrator@ueibi.com
MAIL_PASSWORD=QKwp34MHkHgcz
MAIL_FROM_ADDRESS=administrator@ueibi.com

# Registration workflow
LICENSE_UNIT_PRICE=3999
GST_RATE=0.18
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5
ACTION_TOKEN_TTL_DAYS=14


# Dedicated JWT signing secret
JWT_SECRET=a7f92b490ce09a473187c3e5d0d8be3fae498c19958742d48baae54c59918fb2

# MongoDB Connection (MongoDB Atlas Cloud)
MONGODB_URI="mongodb+srv://pratikparida225_db_user:1SkB6lJL01XfFWqF@cluster0.kpto4td.mongodb.net/ueibi?retryWrites=true&w=majority&appName=Cluster0"

# Apache Kafka & Zookeeper
KAFKA_BROKERS=127.0.0.1:9092
KAFKA_CLIENT_ID=ueibi-service
KAFKA_GROUP_ID=ueibi-gallery-consumer-group
KAFKA_TOPIC_GALLERY=ueibi-gallery-events


# ── Platform Owner bootstrap ────────────────────────────────────────────────
# Used ONLY by `npm run seed:platform-owner`, which creates the internal
# platform tenant and the PLATFORM_OWNER account. Nothing reads these at
# runtime, so they can be removed once the account exists.
#
# PLATFORM_OWNER is the operator of the platform, not a member of any company.
# The seed refuses to convert an existing company account, and no UI or API can
# create this role — this script is the only path.
#
# Change the email/password below to your own before running the seed.
PLATFORM_OWNER_EMAIL=ueibi05@gmail.com
PLATFORM_OWNER_PASSWORD=EJEwsipjbtuNr2fMKjey*-
PLATFORM_OWNER_NAME=Platform Owner
